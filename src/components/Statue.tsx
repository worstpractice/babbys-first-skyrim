import { default as React } from 'react';

type Props = {};

export const Statue = ({}: Props) => {
  return (
    <div
      style={{
        height: 440,
        pointerEvents: 'all',
        width: 250,
      }}
    />
  );
};

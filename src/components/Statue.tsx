import { default as React } from 'react';

type Props = {};

export const Statue = ({}: Props) => {
  return (
    <div
      style={{
        height: 350,
        pointerEvents: 'all',
        width: 200,
      }}
    />
  );
};
